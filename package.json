{
  "name": "auto-reply",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "echo Build successful",
    "start": "npm run start --prefix server",
    "start:prod": "npm run build --prefix client && cross-env NODE_ENV=production SERVE_STATIC=true node server/index.js"
  },
  "dependencies": {
    "puppeteer": "^23.9.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
